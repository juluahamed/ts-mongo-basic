{
  "name": "ts-mongo-basic",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "prestart": "npm run build",
    "start": "ts-node-dev --respawn --pretty --transpile-only src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier . --write"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/juluahamed/ts-mongo-basic.git"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "mongodb": "^4.0.1"
  },
  "devDependencies": {
    "@types/node": "^16.4.7",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.3.5",
    "prettier": "^2.3.2"
  },
  "author": "",
  "license": "ISC"
}
